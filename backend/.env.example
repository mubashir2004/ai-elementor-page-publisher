# Backend env — copy to .env (and load however you prefer, e.g. `export $(cat .env | xargs)`)
# Two-terminal local setup.

PORT=8787
FRONTEND_ORIGIN=http://localhost:5173
SESSION_SECRET=change-me-to-a-long-random-string
NODE_ENV=development
